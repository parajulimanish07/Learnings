
#name = input ("Enter your full name: ")
phone_number = input("Enter your phone number: ")

# result = len(name)

# result = name.find("s") # find the index of the first occurence of the string

# rfind = name.rfind("s") # find the index of the last occurence of the string

#name = name.capitalize() # capitalize the first letter of the string

#name = name.upper() # convert the string to uppercase

#name = name.lower() # convert the string to lowercase

# name= name.isdigit() # check if the string is a digit, sabai digit vaye true hunxa

# result = name.isalpha() # check if the string is an alphabet, sabai alphabet vaye true hunxa

# result = phone_number.count("-") # count the number of occurence of the string

phone_number = phone_number.replace("-", " ") # replace the string with another string (most useful)

print(phone_number)





#print(help(str)) # to get the list of all the string methods